/**
 * docs/contrato-api.md solo confirma idRol=1 para el rol "estudiante"
 * (rol por defecto asignado en el registro). El id del rol "docente" aun
 * no aparece en ningun ejemplo del contrato; se asume 2 hasta que el
 * backend lo confirme (ver docs/decisiones-tecnicas.md).
 */
export const ID_ROL_ESTUDIANTE = 1;
export const ID_ROL_DOCENTE = 2;
